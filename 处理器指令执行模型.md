---
tags:
  - 计算机组成原理
---
# 指令执行方案
![](assets/处理器指令执行模型/file-20260516203657806.png)
%%单指令周期，多指令周期，流水线方案%%
P203
>像单总线，多总线这些方式都是很古老的方式了，只是用来学校，现代计算机都是用单周期，多周期，注意区分。


>单周期CPU：一个指令周期就是一个时钟周期，一个时钟周期已经是一个计算机最小的时间单位，在一个时钟周期内，不管是哪一个部件，它的**控制信号的取值都是不能够改变的**并且**任何一个硬件状态都只能够改变一次**

>流水段寄存器之间的数据由时钟边沿触发，严格对应一个时钟周期