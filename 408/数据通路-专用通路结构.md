![](assets/数据通路-专用通路结构/file-20260520193619453.png)
>M(MAR):M表示主存`M(MAR)` 整体的意思就是：去主存（M）中，找到地址为MAR里所存数值的存储单元，并取出其中的数据。

>单周期处理器（CPI=1,p11）不能采用[数据通路-单总线结构](数据通路-单总线结构.md)的数据通路，因为单总线将所有寄存器都连接到一条公共总线上，一个时钟周期内只允许一次操作，无法完成一条指令的所有操作