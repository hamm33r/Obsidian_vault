---
tags:
  - 计算机组成原理
  - 溢出
---

# 溢出判别方法
## 1.采用一位符号位
![](assets/定点数的加减运算/file-20260330173906365.png)其中$A_s$表示被加数的符号位，$B_s$表示加数的符号位，$S_s$表示运算结果的符号位。
- 根据**性质**：正数+正数，才会上溢——正+正=负
- 只有负数+负数，才会下溢——负+负=正
- $$A_sB_s\bar{S_s}\quad 表示的是正+正=负的情况$$
- $$\bar{A_s}\bar{B_s}S_s表示的是负+负=正的情况$$
## 2. 采用1位符号位并结合进位情况
![](assets/定点数的加减运算/file-20260330180228481.png)
- $C_s$表示符号位相加后向更高位的进位，$C_1$表示符号位上一位向符号位的进位
- 其实就是带标志位加法器[OF（OverFlow Flag）](带标志位加法器.md#OF（OverFlow%20Flag）)里==OF==的判断方法
- 图中的例子A+C是上溢的情况，也就是==正数+正数=负数的情况==
  ![693](assets/定点数的加减运算/file-20260330180459188.png)
## 3. 采用双符号位 
![](assets/定点数的加减运算/file-20260330181201434.png)
- 这里的双符号位，==最高位的符号位表示的是本来运算正确的符号（如正+正=正）,第二个符号位表示实际的符号==二者不相同说明发生了溢出
