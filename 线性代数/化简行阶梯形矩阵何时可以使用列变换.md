在将非零矩阵化为行阶梯形矩阵和行最简阶梯形矩阵时，**只能使用初等行变换，不能使用初等列变换**。这一限制源于行阶梯形矩阵的定义本质及其在线性代数中的应用目的。

---

## 一、核心原因

### 1. **行阶梯形矩阵的定义依赖于行结构**

- 行阶梯形矩阵需满足两个关键条件：
    - 零行必须位于所有非零行的下方；
    - 每个非零行的**首非零元（主元）的列标必须随行标严格递增**。
- 这一结构直接对应线性方程组的消元过程，其核心是**通过行操作逐步消去下方元素**，而非改变列的顺序或组合关系。
- **初等列变换会破坏列的原始含义**（例如交换列相当于重新排列变量顺序），导致无法保持原方程组的解结构不变。

### 2. **初等列变换改变矩阵的列空间**

- 初等行变换保持**行空间**和**解空间**不变，因此适用于求解线性方程组或化简为行阶梯形；
- 初等列变换会改变**列向量之间的线性关系**，导致原矩阵的列空间（即变量间的依赖关系）被破坏，无法正确反映原问题的几何或代数意义。

---

## 二、何时可以使用列变换？

虽然化为行阶梯形时**不能使用列变换**，但在其他场景中列变换是允许的：

### 1. **求矩阵的秩**

- 矩阵的秩在初等行变换和初等列变换下均保持不变，因此**求秩时可混合使用行列变换**（但仅用行变换已足够）。

### 2. **化为矩阵的等价标准形**

- 标准形（如 $\begin{bmatrix} E_r & 0 \\ 0 & 0 \end{bmatrix}$）需要**同时使用行变换和列变换**才能得到，此时列变换是必要的。

### 3. **化为列阶梯形矩阵**

- 若目标是化为**列阶梯形矩阵**（定义类似行阶梯形，但针对列结构），则**只能使用初等列变换**，且与行阶梯形无关。

---

## 三、常见误区澄清

### 1. **行阶梯形 ≠ 列阶梯形**

- 行阶梯形矩阵由**行变换**唯一确定，列阶梯形矩阵由**列变换**唯一确定。两者结构不同，**不能混用变换方式**。

### 2. **求解线性方程组时必须仅用行变换**

- 列变换会改变变量顺序（例如交换第1列和第2列相当于交换变量 $x_1$ 和 $x_2$），导致解的解释错误。因此，**解方程组时必须仅用行变换**。

### 3. **行最简形的唯一性**

- 任意矩阵的**行最简阶梯形是唯一的**（由方程组唯一确定），但若混用列变换，结果将不再符合行阶梯形的定义。

---

## 四、总结

- **化为行阶梯形或行最简阶梯形时，必须仅使用初等行变换**，这是由其定义和应用场景决定的。
- 列变换仅适用于求秩、化标准形或构造列阶梯形等特定任务，**不能用于行阶梯形的化简过程**。
- 若混用列变换，得到的矩阵可能满足数值上的“阶梯”形式，但**已失去行阶梯形矩阵的代数意义**，无法用于求解方程组或分析行空间结构。