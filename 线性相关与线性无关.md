# 部分相关，整体必相关
$$\alpha_1,\alpha_2,线性相关$$
则$$\alpha_1,\alpha_2,\alpha_3...\alpha_s 线性相关$$
>只要这个向量组里，存在向量可以被其他向量表示，也就是他并没有在构成空间的维数当中起到不可缺少的作用，那么就说这一整个组是线性相关的

>这个组线性相关不能推出里面任意一个向量都可以被其他向量表示，因为可能只是其中的一个向量是可以被表示的，剩余的其他向量都是互相线性无关的$$\alpha_1,\alpha_2,\alpha_3...\alpha_s 线性无关$$
>$$\alpha_1,\alpha_2,\alpha_3...\alpha_s,\beta 线性相关$$
>说明这个组里只有$\beta$可以被其他向量线性表示

# 整体无关，部分必无关
$$\alpha_1,\alpha_2,\alpha_3...\alpha_s 线性无关$$
>这个向量组已经构成了s维的空间，去掉任何一个向量都会使这个空间的维数降低，也就是会使秩减1。

>如果新增一个向量，但是并没有影响这个向量组的秩，说明这个向量可以被原向量组线性表示
>如果新增向量$$r(\alpha_1,\alpha_2,\alpha_3...\alpha_s)+1=r(\alpha_1,\alpha_2,\alpha_3...\alpha_s,\beta) $$
>说明新增的这个向量$\beta$不在原向量组所张成的空间当中，是线性无关的。P92


# 方阵
如果是一个方阵
行列式不为0，则线性无关